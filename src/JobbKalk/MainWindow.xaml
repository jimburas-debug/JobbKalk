<Window x:Class="JobbKalk.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="JobbKalk" Width="1180" Height="760" MinWidth="900" MinHeight="620"
        WindowStartupLocation="CenterScreen" Background="{StaticResource CanvasBrush}">
  <Grid>
    <Grid.ColumnDefinitions><ColumnDefinition Width="228"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>

    <Border Grid.Column="0" Background="#153F31">
      <Grid Margin="22,24,18,20">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <StackPanel>
          <Border Width="42" Height="42" CornerRadius="10" Background="#2E8565" HorizontalAlignment="Left">
            <TextBlock Text="JK" Foreground="White" FontSize="17" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>
          <TextBlock Text="JobbKalk" Foreground="White" FontSize="25" FontWeight="SemiBold" Margin="0,13,0,2"/>
          <TextBlock Text="Praktiske beregninger.&#x0a;Helt uten nett." Foreground="#B8CDC4" FontSize="13" LineHeight="20"/>
        </StackPanel>

        <TextBlock Grid.Row="1" Text="FAGOMRÅDER" Foreground="#86AA9B" FontWeight="SemiBold" FontSize="11" Margin="0,34,0,10"/>
        <ListBox x:Name="CategoryList" Grid.Row="2" SelectionChanged="CategoryList_SelectionChanged"
                 Background="Transparent" BorderThickness="0" Foreground="White" FontSize="14">
          <ListBox.Resources>
            <Style TargetType="ListBoxItem">
              <Setter Property="Padding" Value="12,10"/><Setter Property="Margin" Value="0,2"/><Setter Property="Cursor" Value="Hand"/>
              <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
              <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem">
                <Border x:Name="B" CornerRadius="7" Background="Transparent" Padding="{TemplateBinding Padding}"><ContentPresenter/></Border>
                <ControlTemplate.Triggers>
                  <Trigger Property="IsSelected" Value="True"><Setter TargetName="B" Property="Background" Value="#2A654F"/></Trigger>
                  <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="B" Property="Background" Value="#225440"/></Trigger>
                </ControlTemplate.Triggers>
              </ControlTemplate></Setter.Value></Setter>
            </Style>
          </ListBox.Resources>
        </ListBox>

        <Border Grid.Row="3" BorderBrush="#35614F" BorderThickness="0,1,0,0" Padding="0,15,0,0">
          <StackPanel>
            <TextBlock Text="VERSJON 0.1" Foreground="#86AA9B" FontSize="10" FontWeight="SemiBold"/>
            <TextBlock Text="Data sendes aldri ut av PC-en." Foreground="#B8CDC4" FontSize="11" Margin="0,5,0,0" TextWrapping="Wrap"/>
          </StackPanel>
        </Border>
      </Grid>
    </Border>

    <Grid Grid.Column="1" Margin="34,26,34,28">
      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
      <Grid x:Name="HeaderPanel">
        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="330"/></Grid.ColumnDefinitions>
        <StackPanel>
          <TextBlock x:Name="PageTitle" Text="Alle kalkulatorer" FontSize="29" FontWeight="SemiBold"/>
          <TextBlock x:Name="PageSubtitle" Text="Velg et verktøy og fyll inn verdiene." Foreground="{StaticResource MutedBrush}" FontSize="14" Margin="0,5,0,0"/>
        </StackPanel>
        <Border Grid.Column="1" Background="White" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="9" Height="44">
          <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="42"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
            <TextBlock Text="⌕" FontSize="22" Foreground="{StaticResource MutedBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBox x:Name="SearchBox" Grid.Column="1" BorderThickness="0" Background="Transparent" FontSize="14" VerticalContentAlignment="Center"
                     TextChanged="SearchBox_TextChanged" ToolTip="Søk etter kalkulator"/>
          </Grid>
        </Border>
      </Grid>

      <ScrollViewer x:Name="CatalogScroll" Grid.Row="1" Margin="0,24,0,0" VerticalScrollBarVisibility="Auto">
        <WrapPanel x:Name="CalculatorCards" Orientation="Horizontal"/>
      </ScrollViewer>

      <ScrollViewer x:Name="DetailScroll" Grid.Row="0" Grid.RowSpan="2" Visibility="Collapsed" VerticalScrollBarVisibility="Auto">
        <StackPanel MaxWidth="820" HorizontalAlignment="Left">
          <Button Style="{StaticResource SecondaryButton}" HorizontalAlignment="Left" Padding="14,8" Click="Back_Click" Content="←  Alle kalkulatorer"/>
          <TextBlock x:Name="DetailCategory" Foreground="{StaticResource BrandBrush}" FontSize="12" FontWeight="Bold" Margin="0,28,0,6"/>
          <TextBlock x:Name="DetailTitle" FontSize="31" FontWeight="SemiBold"/>
          <TextBlock x:Name="DetailDescription" Foreground="{StaticResource MutedBrush}" FontSize="14" Margin="0,7,0,0" TextWrapping="Wrap"/>

          <Border Background="White" CornerRadius="12" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" Padding="25" Margin="0,24,0,0">
            <StackPanel>
              <TextBlock Text="Verdier" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
              <UniformGrid x:Name="InputGrid" Columns="2"/>
              <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Button Style="{StaticResource PrimaryButton}" Click="Calculate_Click" Content="Beregn" MinWidth="130"/>
                <Button Style="{StaticResource SecondaryButton}" Click="Reset_Click" Content="Nullstill" Margin="10,0,0,0"/>
              </StackPanel>
              <TextBlock x:Name="ErrorText" Foreground="#B42318" FontSize="13" Margin="0,12,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>
            </StackPanel>
          </Border>

          <Border x:Name="ResultBorder" Background="#E7F1EB" CornerRadius="12" Padding="25" Margin="0,16,0,0" Visibility="Collapsed">
            <StackPanel>
              <DockPanel><TextBlock Text="Resultat" FontSize="18" FontWeight="SemiBold" DockPanel.Dock="Left"/>
                <Button x:Name="CopyButton" Style="{StaticResource SecondaryButton}" Padding="12,7" Content="Kopier" DockPanel.Dock="Right" Click="Copy_Click"/>
              </DockPanel>
              <StackPanel x:Name="ResultItems" Margin="0,12,0,0"/>
              <TextBlock x:Name="ResultExplanation" Foreground="{StaticResource MutedBrush}" FontSize="12" Margin="0,10,0,0" TextWrapping="Wrap" Visibility="Collapsed"/>
            </StackPanel>
          </Border>

          <Border x:Name="NoteBorder" Background="#FFF8E7" BorderBrush="#E7D8AC" BorderThickness="1" CornerRadius="9" Padding="16" Margin="0,16,0,0" Visibility="Collapsed">
            <StackPanel Orientation="Horizontal"><TextBlock Text="i" FontWeight="Bold" Foreground="#806515" Margin="0,0,10,0"/>
              <TextBlock x:Name="NoteText" Foreground="#6D5A24" FontSize="12" TextWrapping="Wrap" MaxWidth="740"/>
            </StackPanel>
          </Border>

          <Border x:Name="ElectricalDisclaimer" Background="#F7F8F7" BorderBrush="{StaticResource LineBrush}" BorderThickness="1" CornerRadius="9" Padding="16" Margin="0,16,0,0" Visibility="Collapsed">
            <TextBlock Foreground="{StaticResource MutedBrush}" FontSize="11" TextWrapping="Wrap"
                       Text="Elektroberegninger er veiledende. Kabelvalg, vern, forlegningsmåte, korreksjonsfaktorer, kortslutningsytelse og spenningsfall må verifiseres mot gjeldende regelverk og av kvalifisert personell."/>
          </Border>
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </Grid>
</Window>
